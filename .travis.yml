language: python
python:
  - 2.7
before_install:
  - wget http://reinout.vanrees.org/download/mapnik_1.0_amd64.deb
  - sudo apt-get install python-psycopg2 libgeos-dev python-matplotlib python-pyproj
  - sudo dpkg -i mapnik_1.0_amd64.deb
notifications:
  irc:
    channels: "irc.freenode.org#nens"
    on_success: change
  email: false
install:
  - deactivate
  - python bootstrap.py
  - bin/buildout
script:
  - bin/test
